{
  "name": "unauthorized-construction-monitor",
  "version": "1.0.0",
  "description": "Online monitoring of Unauthorized construction across the city",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npx concurrently --kill-others \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
